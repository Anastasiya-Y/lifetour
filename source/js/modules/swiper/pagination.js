/* const swiperBullets = document.querySelectorAll('.hero__bullet');

const setBullets = (bullets) => {
  bullets.forEach((bullet) => {
    bullet.setAttribute('href', '0');
  });
};

const activateBullets = () => {
  setBullets(swiperBullets);
};

export {activateBullets};*/
